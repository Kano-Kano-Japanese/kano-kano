# Google Calendar / Meet 自動連携 — 1回だけの設定

Supabase側の自動キュー・1分ごとの処理・予約DBへのMeet URL保存はすでに実装済みです。Google側で以下を1回だけ設定すると、以後は管理画面で予約を「確定」にするだけで Calendar と Meet が自動生成されます。

1. https://script.google.com/ を開き、「新しいプロジェクト」を作成します。
2. `Code.gs` の内容を、このフォルダの `Code.gs` で全部置き換えます。
3. 左の「プロジェクトの設定」で「appsscript.json マニフェスト ファイルをエディタで表示する」をONにし、`appsscript.json` をこのフォルダの内容で置き換えます。
4. エディタ左側の「サービス」横の `+` を押し、**Google Calendar API** を追加します（マニフェストで既に追加されている場合は不要です）。
5. 右上 **デプロイ → 新しいデプロイ → 種類: ウェブアプリ** を選びます。
6. 「次のユーザーとして実行」= **自分**、「アクセスできるユーザー」= **全員** にします。
7. 初回のGoogle権限確認でCalendarへのアクセスを許可します。
8. 発行された `/exec` で終わるウェブアプリURLをコピーし、ChatGPTに送ってください。

URLを受け取った後は、SupabaseへURLを登録し、秘密鍵を自動登録して、実際の予約で「確定 → Calendar/Meet生成 → DB保存 → Meetリンク入り確定メール」までテストします。

## セキュリティ
ウェブアプリ自体は外部から到達可能ですが、Supabaseが生成した長い秘密鍵が一致しない要求は拒否します。秘密鍵を自分でコピーしたり、このチャットに貼ったりする必要はありません。
